program recursiva;
var
    resultado: int;

int factorial(n: int) [
    {
        if (n <= 1) {
            return 1;
        } else {
            return n * factorial(n - 1);
        };
    }
];

main {
    resultado = factorial(5);
    print("factorial:", resultado);
}
end
